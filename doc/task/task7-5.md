# 7-5 依存関係違反の警告検知

## 作業詳細
- タスクの `start_date` <= 前タスクの `end_date` となる場合に警告フラグを付与するロジック
- タスクリスト表示時（5-3）に警告フラグを持つ行に警告アイコンを表示

## 完了条件
- 前タスクの終了日以前に開始日が設定されているタスクに警告アイコンが表示される
- 保存自体は可能（警告のみで禁止はしない）
- 依存関係を解消すると警告アイコンが消える
