# 6-2 タスク作成処理

## 作業詳細
- 保存ボタン押下時の必須バリデーション（タスク名）
- 新規作成時の `parent_id`・`sort_order` の自動設定（同階層の末尾）
- DB保存後にタスクリストへ新規タスクを反映
- 依存タスク設定時の開始日自動セット（7-3と連携）

## 完了条件
- タスクが正しい `parent_id`・`sort_order` でDBに保存される
- 保存後にタスクリストに新規タスクが表示される
- タスク名未入力で保存するとエラーが表示される
