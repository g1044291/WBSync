# 7-6 親タスク日付の動的計算

## 作業詳細
- 親タスク（子タスクを持つタスク）の `start_date` / `end_date` をDBに保存せず、表示時に動的計算するロジックの実装
- 直接の子タスクの `start_date` の最小値 → 親の `start_date`
- 直接の子タスクの `end_date` の最大値 → 親の `end_date`
- 子がさらに子を持つ場合の再帰計算

## 完了条件
- 子タスクの日付が変わると親タスクの開始日・終了日の表示がリアルタイムに更新される
- 子タスクが1件もない親タスクは開始日・終了日が「-」表示になる
