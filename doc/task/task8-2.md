# 8-2 タスクバー描画

## 作業詳細
- タスクの `start_date`・`end_date` から、時間軸上のバー左位置・幅を計算
- バー内に進捗率を視覚表現（塗りつぶし割合 or テキスト）
- 子タスクを持つ親タスクと末端タスクで異なるスタイル（色・形状）
- 親タスクの日付は7-6の動的計算値を使用

## 完了条件
- タスクバーが開始日・終了日に対応した位置・幅で描画される
- 進捗率がバーに反映される
- 親タスクと子タスクでスタイルが区別される
