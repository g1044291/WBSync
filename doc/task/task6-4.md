# 6-4 タスク削除処理

## 作業詳細
- 確認ダイアログの表示（対象タスク名・子タスクも削除される旨の警告文）
- 削除実行（DB側のカスケード削除に委任）
- 削除後にタスクリストを再描画

## 完了条件
- 削除前に確認ダイアログが表示される
- 削除後にタスクとその配下の子タスクが一覧から消える
- 削除したタスクを `predecessor_id` として参照していた依存関係が NULL になる
