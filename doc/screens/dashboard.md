---
type: Screen
title: ダッシュボード
description: 担当者別に工数の予定・実績・遅れを集計して一覧表示し、行展開で担当タスクをツリー表示する画面。
tags: [ui, task, assignee, worklog]
resource: ../../src/WBSync/Components/Pages/Dashboard.razor
---

# ダッシュボード

## レイアウト

```
┌──────────────────────────────────────────────────────────────────────┐
│ [←戻る]  プロジェクトA - ダッシュボード                              │  ← ヘッダー
├──────────────────────────────────────────────────────────────────────┤
│ 担当者          予定工数   実績    遅れ                              │
├──────────────────────────────────────────────────────────────────────┤
│ ▽山田           10.0人日   8.5人日  +1.5人日（前倒し）               │
│     フェーズ1                                                        │
│       タスクA                        5.0人日  3.0人日  +2.0人日（前倒し）│
│       タスクB [実績のみ]                       1.0人日  -            │
│ ▷鈴木            6.0人日   7.0人日  -1.0人日（遅れ）                  │
│ ▷佐藤            0.0人日   0.0人日  0人日                            │
├──────────────────────────────────────────────────────────────────────┤
│ 合計            16.0人日  15.5人日  +0.5人日（前倒し）                │
└──────────────────────────────────────────────────────────────────────┘
```

## 要素

| 要素 | 説明 |
|------|------|
| 戻るボタン | [ガントチャート](gantt-chart.md) へ戻る |
| 担当者一覧 | プロジェクトに登録された全担当者を、担当者名の五十音順で表示（実績・担当タスクの有無は問わない） |
| 展開アイコン | 担当者行の左端。クリックで当該担当者のタスクツリーを展開/折りたたみ |
| 合計行 | 一覧末尾に表示する、全担当者の予定工数・実績・遅れの合算行。展開はできない |

## 集計列（担当者行）

| 列名 | 内容 |
|------|------|
| 予定工数 | 担当者ごとの予定工数合計（人日）。算出方法は[担当者別集計](../requirements/effort-management.md#担当者別集計ダッシュボード)を参照 |
| 実績 | 担当者ごとの実績合計（人日、`work_logs.minutes` 合計 ÷ 480） |
| 遅れ | 予定工数合計 − 実績合計。プラスは前倒し（緑）、マイナスは遅れ（赤）で表示 |

数値には単位「人日」を付与して表示する（例：`10.0人日`、`+1.5人日（前倒し）`）。算出不可（`-`）の場合は単位を付けない。

集計対象タスクの条件（担当者設定あり。実績・ステータスは問わない）は[担当者別集計](../requirements/effort-management.md#担当者別集計ダッシュボード)を参照。担当タスクがない担当者は予定工数0・実績0・遅れ0で表示する。

## タスクツリー（行展開）

担当者行を展開すると、その担当者が関わったタスクの一覧を階層構造（ツリー）で表示する。詳細な対象条件・算出方法は[担当者別タスク一覧](../requirements/effort-management.md#担当者別タスク一覧ダッシュボード行展開)を参照。

| 列名 | 内容 |
|------|------|
| タスク名 | 階層に応じてインデント表示。子タスクを持つ行は集計値を表示しない（すべて「-」） |
| [実績のみ]バッジ | 現在このタスクの担当者ではないが、実績を記録したことがあるタスクの行に表示 |
| 予定工数 | 現在の担当タスクのみ表示。[実績のみ]の行は「-」 |
| 実績 | 現在の担当タスクはタスクの実績合計、[実績のみ]の行はこの担当者が記録した分のみ |
| 遅れ | 現在の担当タスクのみ算出。[実績のみ]の行は「-」 |

## 空データ時の表示

プロジェクトに担当者が1人も登録されていない場合、「担当者が登録されていません」と表示する（合計行も表示しない）。

## 関連

* 要件: [効率管理: 担当者別集計](../requirements/effort-management.md#担当者別集計ダッシュボード), [担当者別タスク一覧](../requirements/effort-management.md#担当者別タスク一覧ダッシュボード行展開)
* テーブル: [tasks](../tables/tasks.md), [work_logs](../tables/work_logs.md), [assignees](../tables/assignees.md)
* 呼び出し元: [ガントチャート](gantt-chart.md)
